<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.aymensoft.customviews">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".view.activities.SplashScreenActivity"
            android:theme="@style/Theme.Background.White.FitsSystemWidows"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.popups.NoInternetConnexionPopupActivity" android:theme="@style/AppTheme.Popup"/>

        <activity android:name=".view.activities.HomeScreenActivity"/>
        <activity android:name=".view.activities.MovieDetailsActivity" android:theme="@style/Theme.Background.White.FitsSystemWidows"/>

    </application>

</manifest>